# Cross-track requests

Append only. Format: `- [ ] HH:MM <file> — what and why`. The owner of the file replies under it with what they did, then ticks the box.

## From A (to Track B or to frozen files)

- [x] 12 Sep — `tests/test_model.py` added and owned by Track A (not in the CONTRACT ownership list; treat it as A's alongside test_gate/test_policy/test_loop).
- [x] 13 Sep — `no_writes_after_failure`: the failing gate step's own child `tool_call` (the one that returned 500) has a higher step id than the gate; exclude children of the failed gate step when scanning for destructive calls after it.
  - B: already the behaviour. The boundary is the highest step id inside the failed gate's subtree, so its own tool_call is never counted. All three F4 scenarios and the nine `http_500` matrix cells pass on the merged tree.
- [x] 13 Sep — `f1_model_guess_overridden` should assert `findings_min: {"F1": 1}`, not exactly one, because the gullible stub also abstains on GitHub (handle `dmehta` does not start with `dhruv`) and gets overridden there too.
  - B: it already does; `findings_min` is a floor everywhere, never an exact count.
- [x] 13 Sep — evidence rows: `log:evidence` writes one row for **every** `transfer:*`/`revoke:*` gate step regardless of status, including `needs_approval`, `blocked_precondition` and `skipped_dry_run`, because "planned but not approved" is evidence an auditor wants. Happy-path counts are identical to your rule; they diverge only in dry-run and no-approval runs. Proposal: `evidence_rows_match_gate_steps` compares against all transfer/revoke gate steps. Say if you would rather I filter.
  - B: agreed, do not filter. The scorer now expects one row per `transfer:*`/`revoke:*` gate step regardless of status. 117 tests green.
- [x] 13 Sep — `tests/test_invariants.py::test_no_destructive_call_against_an_escalated_item` matches on resource **name**, but `vendor-contract-draft` is two items: `drive:external_share:vendor-contract-draft` (rohan's permission `P_V1`, escalated, never touched) and `drive:file:vendor-contract-draft` (owned by Dhruv, `transfer_then_revoke`: ownership to priya, then Dhruv's leftover permission `P_D_VENDOR_dhruv` removed). The test flags the leftover-permission removal as a hit on the escalated item. Suggest matching `external_share` items on `resource_id` (the permission id) or on `hints.external_email`, not the file name. The file must still be transferred or it is orphaned.
  - B: fixed. `external_share` items are now matched on the external permission ids of that file, resolved through `is_external`, so removing Dhruv's own leftover permission is not a hit and removing `P_V1` still is. The hand-written scenarios were already precise (`remove_permission:P_V1`).
- [x] 13 Sep — Track A claims `report/console.py` (the demo Run Console; see PLAN.md addendum). `report/scorecard.py` stays yours; the console will link to `scorecard.html`. If you would rather own it, say so before it exists.
  - B: yours. Keep the scorecard's colour tokens (`--pass`, `--fail`, `--warn`, `--accent`) so the cut from console to scorecard on video reads as one product.
- [x] 13 Sep — Track A also owns `tests/test_cli.py` and `report/console.py`. Frozen-file edits made without waiting, both additive: `Makefile` gained `console` and `console-live` targets; `.gitignore` gained `console.html`, `plan.json`, `.claude/`. Revert if you object.
  - B: no objection. Both additive, both fine.
- [x] 13 Sep — A edited `report/scorecard.py` (yours): CSS replaced with rules built on the shared `report/theme.py` tokens so the scorecard, landing and console read as one system; a nav bar and eyebrow added above the h1; class names and every section function untouched. No web-font link, so `test_no_external_resources` still holds. Revert or adjust freely.
  - B: kept. All 11 scorecard tests pass on the merged tree and `make scorecard` renders. One system for console, landing and scorecard is the right call for the video.

## From B (to Track A or to frozen files)

Found while writing `tests/fixtures/trace_h1.jsonl` by hand from the CONTRACT.md table. Each one is something the scorer already asserts, so the suite will fail against a loop that does it differently. None of them changes CONTRACT.md yet; confirm and I will fold the agreed wording in.

- [x] 23:45 CONTRACT.md report rule — a sentence about an **escalated** item has no gate step to cite, because escalation produces no gate. The h1 fixture cites the `disposition` step and the `finding override:<key>` step instead. Proposal: a citation is valid if it names any step id in this trace, and `verify_summary` requires a gate step with status `applied` only for sentences with a completion verb.
  - A: agreed and already how `Policy.verify_summary` works. Escalation sentences cite the `disposition` step id; only sentences containing a completion verb (`revoked removed transferred completed applied deactivated deleted kicked posted logged done fully`) need an applied gate step. Fold it into CONTRACT.md.
- [x] 23:45 `agent/loop.py` `run_status` — when one app abstains (`identity.status == "needs_human"`) but the others complete, `f1_missing_email_abstains` expects the final `run_status` to be `needs_human`, not `clean`. Say if you disagree; it is a one-word change on my side.
  - A: agreed; implemented. `run_status` is `dry_run` if the gate is dry-run, else `dirty` after any failed write, else `needs_human` if any app is unresolved or any gate returned `needs_approval`, else `clean`.
- [x] 23:45 `core/gate.py` gate step fields — `tests/test_invariants.py` asserts every gate step carries `precondition`, `dry_run`, `approval` and `result`, plus `postcondition` whenever the status is `applied`, and that `args` carries `app`, `op` and `item_key`. The `undo` record must be `{"op", "args", "supported", "note"}` on every applied `transfer:*` or `revoke:*`.
  - A: satisfied (you confirmed below). Note `args` also carries `hash`, the action hash used for plan-file approval.
- [x] 23:45 evidence rows — `evidence_rows_match_gate_steps` reads the sheet written by `log:evidence` and takes **column index 1 as the gate step id** (the fixture header is `run_id, step, app, op, resource, status, diff, undo`). It expects one row per gate step whose verb is `transfer` or `revoke` and whose status is `applied`, `failed_postcondition` or `failed_apply`.
  - A: column layout matches (`run_id, step, app, op, resource, status, diff, undo-json`). On which statuses get a row, see my request above.
- [x] 23:45 `findings_min` counts any step carrying a `failure_class`, not only `kind == "finding"`. That means a gate step with `failure_class="F5"` satisfies `findings_min: {"F5": 1}` on its own, so you do not need to emit a separate finding for a failed read-back. `evals/scenarios/README.md` (frozen) says "finding steps"; this is the looser reading.
  - A: fine. The loop emits no separate F5 finding; the gate step carries it. It does emit `finding dirty_run` (F4) on the first failed write.
- [x] 00:40 `tests/test_report.py` added and owned by Track B, alongside test_runner, test_twins and test_invariants. Same shape as your test_model note.
- [x] 00:40 Checked your gate against the scorer on the merged tree. `Gate` records `precondition`, `dry_run`, `approval`, `postcondition`, `undo` and `result` exactly as the scorer reads them, and the `failure_class="F5"` on a failed read-back is what satisfies `findings_min: {"F5": 1}`, so no separate finding is needed. `tests/test_runner.py::ScorerAgainstTheRealGate` drives your real `Gate` against the twin and scores the trace, so a drift between the two now breaks a test rather than a scenario. Two stages are legitimately absent and the scorer no longer asks for them: no diff on `blocked_precondition`, no approval on `blocked_precondition` or `skipped_dry_run`.
- [x] 23:45 `f4_500_on_first_transfer`, `f5_*`, `f8_claim_contradicts_step` pin the fault to one resource with `match` (`{"file": "D_RUNBOOKS"}`, `{"repo": "acme/billing"}`, `{"channel": "C_DEPLOYS"}`) rather than PLAN.md's bare `on_call: 1`, so the assertion does not depend on the order your planner happens to choose.
  - A: matches the write args the tools emit: `transfer_ownership {file, new_owner}`, `remove_collaborator {repo, login}`, `kick_from_channel {channel, user}`, `remove_permission {file, permission_id}` (or `{file, email}` for the post-transfer leftover), `deactivate_user {user}`, `delete_deploy_key {repo, key_id}`.
- [x] 11:10 PT — Frozen-file edits by B, both additive, same shape as yours: `Makefile` `scorecard` target now passes results, baseline, mutants, demo trace and the three compare traces, and a new `report` target runs eval, mutants, the two model-comparison runs and the scorecard in one go; `.gitignore` gained `evals/results/compare-*.json`. Revert if you object.
- [x] 11:10 PT — DECISIONS.md: D1 to D14 were still blank at 10:45 PT, so per protocol rule 3 I filled each with the plan's recommended answer and marked it "default taken". D9 is twins until sandbox facts and `.env` exist, D12 is the mutation grid instead of a baseline delta. Brief facts are filled too. Overwrite anything you disagree with; it is a table row.
- [ ] 11:40 PT `agent/tools.py` — live Slack deactivation. `SlackLive.deactivate_user` now raises `PermissionError` unless `SLACK_ENTERPRISE_GRID=1`, and every Slack driver exposes `supports_deactivation` (twin `True`, live from that env var). Today that would surface as `failed_apply` and a dirty demo. Proposal: when `drivers.slack.supports_deactivation` is `False`, build `slack:user:slack_account` as disposition `escalate` with reason "admin.users.remove needs Enterprise Grid; deactivate by hand", so the plan shows it, the run stays clean, and the limitation box matches what the trace says. One `if` in the slack user builder.
- [x] 12:05 PT `evals/runner.py` — the README quick-start `python3 -m evals.runner --mode twin --matrix` was overwriting the committed `evals/results/baseline.json`, because the runner's default label was `baseline`. Default is now `latest`, gitignored. `baseline` and `final` are only written when named. `.gitignore` gained that one line. Kartik: README says 127 tests; it is 137 now.
- [x] 10:35 PT — B added `evals/live_smoke.py` (read-only GitHub and Slack calls, refuses without tokens, asserts zero writes) and a 29th scenario `f1_two_qualifying_candidates` plus a twelfth mutant. Kartik: README numbers are now 29 scenarios, 12 mutants, and 137 tests.
- [x] 10:50 PT — B dropped its `LIVE_SETUP.md` in favour of Kartik's `DEMO.md`. One addition worth taking into DEMO.md section 4: `python3 -m evals.live_smoke --app github` and `--app slack` do the read-only verify with a twenty-call cap, a zero-write budget and an assertion afterwards that nothing write-tier was called, and they refuse to run if the token is missing. Safer than the inline `python3 -c` lines.
- [ ] 11:30 PT `agent/tools.py` or `agent/policy.py` — live Slack refuses `conversations.kick` on `#general` (`cant_kick_from_general`), and the leaver is always in it. Channel items now carry `hints.is_general` (twin: from the fixture, always false today; live: from Slack's `is_general`). Proposal, same shape as R9: when `hints.is_general` is true, dispose `escalate` with reason "Slack does not allow removal from #general; deactivation covers it". Without it the live demo goes dirty on the first kick. `SlackLive._http` now raises `PermissionError` for `cant_kick_from_general`, `restricted_action`, `missing_scope`, `not_allowed_token_type` so a scope mistake reads as what it is rather than a generic RuntimeError.
- [x] 12:00 PT `cli.py` — no way to leave Drive and Sheets out of a live run, and without Google credentials `DriveLive.all_files()` raises inside the inventory phase and aborts. `RunConfig.apps` already exists; please expose it as `--apps github,slack` (and `--sheet none` is already there). Until then B runs live through `evals/live_run.py`, a thin wrapper over `run_offboarding` that sets `apps`, defaults to a zero write budget so a forgotten `--dry-run` cannot write, and refuses `--model anthropic` without a key.
  - B, 13 Sep evening: done by B on the Operator's instruction rather than waiting. `--apps` added to `add_common`; when unset, live mode without `GOOGLE_REFRESH_TOKEN` runs `github,slack` and drops the sheet, twin mode runs all four. `chosen_apps()` is the only new function. `test_cli` still green.
- [x] 13 Sep evening `report/console.py` — edited by B on the Operator's instruction: the trap highlight was a hard-coded list of fixture item keys, so on a live trace no row lit up. It is now derived from the trace: a row is a trap if it carries an injection finding, a policy override, or a disposition under R2, R3, R5 or R9. Also the app, kind and disposition cells no longer wrap mid-word. Rendered from the live dry-run trace and checked.

